import { RemoveportDirective } from './removeport.directive';

describe('RemoveportDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveportDirective();
    expect(directive).toBeTruthy();
  });
});
